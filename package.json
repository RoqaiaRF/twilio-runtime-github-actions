{
  "name": "twilio-ci-test",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run --env"
  },
  "devDependencies": {
    "twilio-run": "^2.0.0",
    "axios": "^0.26.1",
    "query-string": "^7.1.1"
  },
  "engines": {
    "node" : ">=8.0.0 <11.0.0" 
  }
}
